#EXCEL AUTOMATIC UPDATE

#Used to automatically open .xlsb files, go through all the sheets and RefreshAll data without user intervention

#The scrips checks automatically for pywin32 module, can be removed if the condition is true
